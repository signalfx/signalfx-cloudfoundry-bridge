set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.8)
export PATH=$GOROOT/bin:$PATH

cd signalfx_agent

export GOPATH=$PWD

cd src/github.com/signalfx/cloudfoundry-integration

go install
cp -a $GOPATH/bin/cloudfoundry-integration ${BOSH_INSTALL_TARGET}
